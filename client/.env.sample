CLIENT_URL="https://oauth-mcp-client.local"
HTTPS_PROXY="http://localhost:8888"
HTTP_PROXY="http://localhost:8888"
LOGFIRE_ENABLED="true"
LOGFIRE_TOKEN="••••"
MCP_SERVER_URL="https://app.getgram.ai/mcp/your-server"
NO_PROXY="localhost,127.0.0.1"
OPENAI_API_KEY="••••"
OPENAI_MODEL="gpt-5-mini"
OTEL_PYTHON_EXCLUDED_URLS=".*health-check,.*__reload__,.*static,.*media,.*__debug__/.*,.*well-known"
PYTHONPATH="."
SYSTEM_PROMPT="You are a focused AI assistant.
 • Always answer clearly and concisely.
 • When a user request requires external knowledge, prefer using MCP server tools.
 • Only call tools when necessary, and explain results in plain language.
 • If no tool is suitable, reason from your own knowledge.
 • Never invent tool results or capabilities.
 • Show your reasoning to the user only when explicitly asked.
 • Keep responses helpful, actionable, and minimal" 